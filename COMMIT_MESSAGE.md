# Commit Message

```
feat: 大幅升级体验——深色模式、备份恢复、年份折叠置顶、草稿自动保存、标签、分享图片

## 全局（js/app.js + css/theme.css）
- 新增深色模式：右上角按钮一键切换，首次进入自动跟随系统偏好，全部语义 Token 整体映射（含情绪色板提亮、底部导航栏底色）
- 新增完整备份/恢复：列表页「备份全部」导出 JSON，点「恢复备份」先弹确认再选择文件导入（含条目校验、自动置种子标记）
- 新增基于 Promise 的自定义确认弹层（modal），替换列表页、历史页、编辑页所有 window.confirm，深色/浅色均适配，带危险操作红色样式

## 列表页（diary-list.html）
- 日记自动按年份分组折叠，仅展开当年，可点击展开/收起更早年份；年份头显示篇数
- 新增置顶功能：卡片图钉按钮，置顶日记固定在列表最前
- 新增记录概览卡片：总篇数 / 连续记录天数 / 本月篇数
- 新增标签展示：#标签 徽章；搜索与年份折叠/排序兼容

## 编辑器（diary-editor.html）
- 草稿自动保存：标题/正文/天气/心情/标签输入即存 localStorage，误关重开自动恢复，保存成功自动清除
- 标签字段：编辑器可填标签（逗号分隔），保存/编辑回显/分享卡均支持
- 字号统计：正文下方实时显示字数
- 新增「小标题」「引用」排版格式，补充 h3/引用/列表/内嵌图片样式
- 新增分享图片：把日记渲染成 1080×1440 竖版卡片 PNG 下载（心情主题色渐变背景 + 标题/日期/天气/心情/正文/标签/品牌页脚，画布纯本地生成，零外部依赖）
- 删除日记改用自定义确认弹层

## 历史页（diary-history.html）
- 删除日记改用自定义确认弹层

## 分析页（diary-analytics.html）
- 自身补齐主题切换按钮

## 文档
- README 功能表与 FAQ 同步新增年份折叠、置顶、概览、标签、草稿自动保存、深色模式、备份恢复、分享图片等说明
```