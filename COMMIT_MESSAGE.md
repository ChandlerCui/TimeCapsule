# Commit Message

```
feat: 编辑页新增一键导出 PDF 功能，A4 精排模板支持富文本、照片与分页

## 新功能：导出 PDF（diary-editor.html）

- 编辑页底部操作栏新增「导出 PDF」按钮，一键把当前日记（含未保存的草稿）排版成 A4 文档并下载
- 文件名自动生成为「时光胶囊_日期_标题.pdf」，标题中的非法字符自动清理
- 精心设计的 A4 排版模板：品牌页眉 + 导出日期、日记大标题、日期/天气/心情元信息行、渐变分隔线、正文、照片与图片说明、音频附件标签、品牌页脚
- 正文保留富文本格式：加粗、斜体、列表、正文内嵌图片均正常渲染
- 图片支持旋转角度还原（90°/180°/270°），说明文字同步导出
- 录音/音频以胶囊标签形式列出（♪ 名称），提示在应用内播放
- 多页日记自动分页，照片与音频卡片设置 break-inside: avoid，不会被页边界切断

## 技术实现

- 引入 html2pdf.js（html2canvas + jsPDF）本地 vendor 至 js/vendor/，保持「零外网依赖、本地优先」原则，断网可用
- 库文件按需动态加载（点击导出时才加载，12 秒超时保护），不影响编辑页首屏性能
- 渲染节点以 opacity:0 绝对定位挂载，用户全程无感；通过 html2canvas onclone 回调在克隆文档中恢复可见性渲染
- 修复两处渲染缺陷：
  · 移除 html2canvas 的 windowWidth 选项（该选项在部分视口下导致画布裁剪坐标错位、内容被裁切）
  · 渲染前将页面滚动位置复位到顶部（html2canvas 渲染受页面滚动影响），导出完成后恢复原位
- 等待全部图片加载完成后再渲染（8 秒超时兜底），避免图片空白
- 三层容错：库加载失败或渲染异常时自动回退到 window.print() 打印面板（选择「存储为 PDF」），打印样式仅输出排版节点
- 导出过程按钮防重复点击（禁用 + 状态文案），完成后 toast 反馈结果

## 文档

- README.md 功能表新增「导出 PDF」说明，FAQ 新增 PDF 保存位置与离线可用性问答
```
