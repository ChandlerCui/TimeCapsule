# Commit Message

```
feat: 新增历史日历、录音、心情分析页面，修复删除bug，重设计柔和iOS风格

## Bug 修复

- 修复详情页删除日记后返回列表仍可见的问题
  · 编辑器删除后调用 TC.deleteEntry() 立即写入 localStorage，并延迟跳转回列表页
  · 列表页读取数据时始终从 localStorage 重新加载，确保状态同步
- 首页列表新增内联删除按钮（hover 显示，移动端常驻）
  · 点击删除弹出二次确认，确认后即时调用 deleteEntry 并重新渲染列表
  · 无需跳转详情页即可完成删除操作

## 新功能

### 历史页面（diary-history.html）
- 新增日历视图，支持按月翻页浏览
- 有日记的日期显示情绪色点标记，点击日期查看当天所有记录
- 保留原有列表浏览模式，日历与列表双模式并存
- 底部导航栏串联「日记 / 历史 / 分析」三个页面

### 录音功能（diary-editor.html）
- 新增「开始录音」按钮，点击即调用 MediaRecorder API 直接录制
- 录音中按钮显示脉冲动画，再次点击停止并自动保存
- 新增「上传音频」按钮，支持选择本地音频文件
- 音频以卡片形式展示，内嵌播放器，支持单独删除

### 分析页面（diary-analytics.html）
- 情绪折线图：Canvas 绘制，展示一段时间内情绪值起伏趋势
- 词云组件：从日记正文提取高频词，字号和颜色随频率变化
- 情绪占比饼图：Canvas 绘制环形图，各类情绪出现频率一目了然
- 顶部统计概览卡片：总日记数、本月篇数、连续天数、主要情绪
- 全部图表使用原生 Canvas + CSS 实现，零外部依赖

### 预生成示例数据
- 首次进入应用时自动写入 6 篇示例日记
- 涵盖不同情绪、天气、图片，帮助用户快速理解用法
- 通过 localStorage 标记位确保仅首次写入，不覆盖用户数据

## 设计重构

### 配色方案（theme.css）
- 整体改为柔和舒缓的 iOS 风格
- 主色调：奶白（#FAF8F5）作为背景基底
- 辅助色：雾紫（#B8A9D4 / #C4B5D9）与淡蓝（#9DBBD4 / #A8C8E0）
- 主交互色：柔紫蓝渐变，饱和度低，视觉不刺激
- 卡片、圆角、阴影均参考 iOS 系统组件规范

### 动效优化
- 全局过渡曲线统一为 cubic-bezier 柔和缓动
- 卡片 hover 微浮起、按钮 active 缩放反馈
- 页面切换、列表渲染添加淡入动画
- 支持 prefers-reduced-motion，尊重无障碍偏好

## 技术变更

- 新增 js/app.js：共享数据层（CRUD + 种子数据 + 情绪/天气定义 + 词频提取 + 底部导航）
- 新增 css/theme.css：统一设计令牌（颜色 / 圆角 / 阴影 / 动效 / 响应式）
- 所有页面引用共享 CSS 和 JS，消除重复样式
- 底部 Tab Bar 导航组件统一管理
```
